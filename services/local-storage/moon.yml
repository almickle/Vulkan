layer: "application"
language: "typescript"

tasks:
  ### Persistent tasks ###
  host:
    command: "tsx"
    args: ["./src/index.ts"]
    preset: "server"
    deps:
      - "^:lint"
      - "^:type-check"
      - "^:build"
      - "lint"
      - "type-check"
